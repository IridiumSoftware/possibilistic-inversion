# Cross-audit — 2026-06-10 (Demonstration 4 / PoroTomo arc)

Scope: §7 of `possibilistic_tomography_note.md` (Demonstration 4), its
figures, `docs/porotomo_3d_companion.md`, and the cert/JSON artifacts.
Method: every numeric claim in §7 mechanically re-checked against the
committed certs (`porotomo_*.json`); every cross-reference read at its
target; note↔companion compared for drift.

## Verified clean (18 of 19 mechanical checks exact)

Decomposition fractions (7.7/0.1/13.1/79.1% at eps 0.25), illumination
(20.7%), width ordering (0.568/1.498), holdout (26.8%/65.4%, 53.9 ms,
38 ms), three-way table (all 12 cells), sensitivity ceiling/variants
(0.701, 0.67, 0.36/0.302), pick/source/node counts, grid dims, member
RMS range, repeat-scatter numbers, GDR DOIs (924=10.15121/1787666,
1124=10.15121/1501544), Parker DOI 10.1785/0220180085. The 19th check
(79.1% vs 0.7905) is rounding, not drift.

## Findings (all fixed in the same commit)

| # | Where | Finding | Fix |
|---|---|---|---|
| F1 | note §7.3 | "six Vp models from three groups and three methods" — ID10≡ID13 are byte-identical in the published grid (five distinct models), and Thurber/Nayak are one method family (first-arrival/body-wave tomography), Matzel the second | restate: six columns, five distinct models, three groups, two method families; duplicate noted |
| F2 | note §7.3 | "medians matching ours to 0.01 km/s" — true only of Thurber 2017-11 (2.357 vs 2.361); family range is 2.34–2.43 | scoped: closest model to 0.004 km/s, family within 0.07 |
| F3 | note §7.3 | "r ≈ 0.92" — actual range 0.89–0.92 | state the range |
| F4 | note Fig 12 caption | "three travel-time/body-wave vintages" inconsistent with six panels | caption corrected, duplicate named |
| F5 | note §7.5 | sensitivity bullet reported corr_600m (J=0.67) but omitted corr_150m (J=0.46, also below ceiling) — selective | both directions reported |
| F6 | note §7.5 + companion | "~1.8× the null rate" imprecise: forced-slower rate ratio is 2.0× (2.45% vs 1.24%), total forced ratio 1.6× | precise rates given |
| F7 | note §7.5 | "neither the deterministic nor any Bayesian treatment has posed" — unbounded negative-existence claim | scoped to "we found none in the published record" |
| F8 | note §7 intro | "named as the decisive extension" — §6.5 says "natural extension" | aligned |
| F9 | companion §2b | geology bullet still asserts "geologically structured, not acquisition-geometry noise" without the shallow-illumination confound caveat the note now carries | caveat propagated |
| F12 | note §7.2 | cross-ref "§8 point 1" for the ensemble-mean-gauge tautology is wrong — §8 point 1 is damping-strength ensemble bias; the gauge retraction lives in §6.2–6.3 | ref corrected to §6.3 |

(F10 merged into F6; F11 — the "~20×" member-cost claim — was checked and
retained: first-ensemble member wall-times 617–1074 s vs 32–48 s after the
fix, median ratio ≈19×.)

## Notes

- The pre-existing forward reference in §6.4 to Figure 14 (sensitivity)
  is intentional and survived the renumber correctly.
- `porotomo/data/` artifacts (ensembles, downloaded GDR files) are
  gitignored by design; all re-fetchable by URL/seed, paths recorded in
  the companion §1.
- HTML rebuilt after fixes; TeX mirror of §7 still pending (tracked as
  the next sync step, matching the §6 flow).
